# Copy to .env for local development. On Render, set these in the Static Site env vars.
VITE_API_URL=http://localhost:5000/api
VITE_LOG_SERVER_URL=http://localhost:5001
# Optional — must match API_KEY on the Flask backend when set
# VITE_API_KEY=
